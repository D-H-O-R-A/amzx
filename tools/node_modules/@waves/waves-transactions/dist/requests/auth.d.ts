import { IAuthParams, IAuth } from '../transactions';
import { TPrivateKey } from '../types';
export declare const serializeAuthData: (auth: {
    host: string;
    data: string;
}) => import("@waves/ts-lib-crypto").TBytes;
export declare function auth(params: IAuthParams, seed?: string | TPrivateKey, chainId?: string | number): IAuth;
