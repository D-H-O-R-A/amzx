import { ICancelOrderParams, ICancelOrder } from '../transactions';
import { TPrivateKey } from '../types';
export declare const cancelOrderParamsToBytes: (cancelOrderParams: {
    sender: string;
    orderId: string;
}) => import("@waves/ts-lib-crypto").TBytes;
export declare function cancelOrder(params: ICancelOrderParams, seed?: string | TPrivateKey): ICancelOrder;
