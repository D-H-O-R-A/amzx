import { CommitToGenerationTransaction } from '@waves/ts-types';
import { ICommitToGenerationParams, WithId, WithProofs, WithSender } from '../transactions';
import { TSeedTypes } from '../types';
export declare function commitToGeneration(params: ICommitToGenerationParams, seed: TSeedTypes): CommitToGenerationTransaction & WithId & WithProofs;
export declare function commitToGeneration(paramsOrTx: ICommitToGenerationParams & WithSender | CommitToGenerationTransaction, seed?: TSeedTypes): CommitToGenerationTransaction & WithId & WithProofs;
