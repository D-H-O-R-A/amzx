"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.sharedKey = exports.messageDecrypt = exports.messageEncrypt = exports.aesDecrypt = exports.aesEncrypt = void 0;
// @ts-ignore
const forgeCipher = __importStar(require("node-forge/lib/cipher"));
// @ts-ignore
const util = __importStar(require("node-forge/lib/util"));
const random_1 = require("./random");
const param_1 = require("../conversions/param");
const hashing_1 = require("./hashing");
const concat_split_1 = require("./concat-split");
const axlsign_1 = __importDefault(require("../libs/axlsign"));
const string_bytes_1 = require("../conversions/string-bytes");
const aesEncrypt = (data, key, mode = 'CBC', iv) => {
    const cipher = forgeCipher.createCipher(`AES-${mode}`, (0, string_bytes_1.bytesToString)((0, param_1._fromIn)(key), 'raw'));
    cipher.start({ iv: iv && util.createBuffer((0, string_bytes_1.bytesToString)((0, param_1._fromIn)(iv), 'raw')) });
    cipher.update(util.createBuffer((0, string_bytes_1.bytesToString)(data, 'raw')));
    cipher.finish();
    return (0, string_bytes_1.stringToBytes)(cipher.output.getBytes(), 'raw');
};
exports.aesEncrypt = aesEncrypt;
const aesDecrypt = (encryptedData, key, mode = 'CBC', iv) => {
    const decipher = forgeCipher.createDecipher(`AES-${mode}`, (0, string_bytes_1.bytesToString)((0, param_1._fromIn)(key), 'raw'));
    decipher.start({ iv: iv && util.createBuffer((0, string_bytes_1.bytesToString)((0, param_1._fromIn)(iv), 'raw')) });
    const encbuf = util.createBuffer((0, string_bytes_1.bytesToString)((0, param_1._fromIn)(encryptedData), 'raw'));
    decipher.update(encbuf);
    if (!decipher.finish()) {
        throw new Error('Failed to decrypt data with provided key');
    }
    return (0, string_bytes_1.stringToBytes)(decipher.output.getBytes(), 'raw');
};
exports.aesDecrypt = aesDecrypt;
const messageEncrypt = (sharedKey, message) => {
    const version = Uint8Array.from([1]);
    const CEK = (0, random_1.randomBytes)(32);
    const IV = (0, random_1.randomBytes)(16);
    const m = (0, string_bytes_1.stringToBytes)(message);
    const Cc = (0, exports.aesEncrypt)(m, CEK, 'CTR', IV);
    const Ccek = (0, exports.aesEncrypt)(CEK, sharedKey, 'ECB');
    const Mhmac = (0, hashing_1.hmacSHA256)(m, CEK);
    const CEKhmac = (0, hashing_1.hmacSHA256)((0, concat_split_1.concat)(CEK, IV), sharedKey);
    const packageBytes = (0, concat_split_1.concat)(version, Ccek, CEKhmac, Mhmac, IV, Cc);
    return packageBytes;
};
exports.messageEncrypt = messageEncrypt;
const messageDecrypt = (sharedKey, encryptedMessage) => {
    const [version, Ccek, _CEKhmac, _Mhmac, iv, Cc,] = (0, concat_split_1.split)(encryptedMessage, 1, 48, 32, 32, 16);
    const CEK = (0, exports.aesDecrypt)(Ccek, sharedKey, 'ECB');
    const CEKhmac = (0, param_1._fromIn)((0, hashing_1.hmacSHA256)((0, concat_split_1.concat)(CEK, iv), (0, param_1._fromIn)(sharedKey)));
    const isValidKey = CEKhmac.every((v, i) => v === _CEKhmac[i]);
    if (!isValidKey)
        throw new Error('Invalid key');
    const M = (0, exports.aesDecrypt)(Cc, CEK, 'CTR', iv);
    const Mhmac = (0, param_1._fromIn)((0, hashing_1.hmacSHA256)(M, CEK));
    const isValidMessage = Mhmac.every((v, i) => v === _Mhmac[i]);
    if (!isValidMessage)
        throw new Error('Invalid message');
    return (0, string_bytes_1.bytesToString)(M);
};
exports.messageDecrypt = messageDecrypt;
const sharedKey = (privateKeyFrom, publicKeyTo, prefix) => {
    const sharedKey = axlsign_1.default.sharedKey((0, param_1._fromIn)(privateKeyFrom), (0, param_1._fromIn)(publicKeyTo));
    const prefixHash = (0, hashing_1.sha256)((0, param_1._fromRawIn)(prefix));
    return (0, hashing_1.hmacSHA256)(sharedKey, prefixHash);
};
exports.sharedKey = sharedKey;
//# sourceMappingURL=encryption.js.map