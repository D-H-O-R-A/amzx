"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.randomSeed = exports.randomBytes = exports.random = void 0;
// @ts-ignore
const random_1 = __importDefault(require("node-forge/lib/random"));
const seed_words_list_1 = require("./seed-words-list");
const string_bytes_1 = require("../conversions/string-bytes");
const _random = (count) => (0, string_bytes_1.stringToBytes)(random_1.default.getBytesSync(count), 'raw');
const ensureBuffer = () => {
    try {
        const b = new Buffer(1);
    }
    catch (e) {
        throw new Error('Buffer not supported in this environment. Use Node.js or Browserify for browser support.');
    }
};
const random = (count, type) => {
    switch (type) {
        case 'Array8':
            return Array.from(_random(count));
        case 'Array16':
            return Array.from((0, exports.random)(count, 'Uint16Array'));
        case 'Array32':
            return Array.from((0, exports.random)(count, 'Uint32Array'));
        case 'Buffer':
            ensureBuffer();
            return Buffer.from(_random(count));
        case 'Uint8Array':
            return _random(count);
        case 'Uint16Array':
            return new Uint16Array(count)
                .map(_ => _random(2).reduce((a, b, i) => a | b << 8 * (1 - i), 0));
        case 'Uint32Array':
            return new Uint32Array(count)
                .map(_ => _random(4).reduce((a, b, i) => a | b << 8 * (1 - i), 0));
        default:
            throw new Error(type + ' is unsupported.');
    }
};
exports.random = random;
const randomBytes = (length) => (0, exports.random)(length, 'Uint8Array');
exports.randomBytes = randomBytes;
const randomSeed = (wordsCount = 15) => (0, exports.random)(wordsCount, 'Array32')
    .map(x => seed_words_list_1.seedWordsList[x % seed_words_list_1.seedWordsList.length])
    .join(' ');
exports.randomSeed = randomSeed;
//# sourceMappingURL=random.js.map