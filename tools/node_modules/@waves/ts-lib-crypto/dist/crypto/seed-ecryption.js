"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.decryptSeed = exports.encryptSeed = void 0;
const string_bytes_1 = require("../conversions/string-bytes");
// @ts-ignore
const forgeMd5 = __importStar(require("node-forge/lib/md5"));
const concat_split_1 = require("./concat-split");
const encryption_1 = require("./encryption");
const base_xx_1 = require("../conversions/base-xx");
const hashing_1 = require("./hashing");
const random_1 = require("./random");
function strengthenPassword(password, rounds = 5000) {
    while (rounds--) {
        const bytes = (0, string_bytes_1.stringToBytes)(password);
        password = (0, base_xx_1.base16Encode)((0, hashing_1.sha256)(bytes));
    }
    return password;
}
function evpKdf(passphrase, salt, output = 48) {
    const passPlusSalt = (0, string_bytes_1.bytesToString)((0, concat_split_1.concat)(passphrase, salt), 'raw');
    let key = '';
    let final_key = key;
    while (final_key.length < output) {
        key = forgeMd5.create().update(key + passPlusSalt).digest().getBytes();
        final_key += key;
    }
    return final_key;
}
/**
 * Encrypts arbitrary utf8 string with utf8 password. Evp key deriving function is used to get encryption key and IV from
 * password. AES-CBC algorithm is used for encryption. Output format is base64 encoded OpenSSL
 * @param seed - utf8 string to encrypt
 * @param password - utf8 password
 * @param encryptionRounds - how many times password will be hashed. Default = 5000
 */
const encryptSeed = (seed, password, encryptionRounds) => {
    const passphrase = strengthenPassword(password, encryptionRounds);
    const salt = (0, random_1.randomBytes)(8);
    const key_iv = evpKdf((0, string_bytes_1.stringToBytes)(passphrase, 'raw'), salt);
    const key = (0, string_bytes_1.stringToBytes)(key_iv.slice(0, 32), 'raw');
    const iv = (0, string_bytes_1.stringToBytes)(key_iv.slice(32), 'raw');
    const encrypted = (0, encryption_1.aesEncrypt)((0, string_bytes_1.stringToBytes)(seed), key, 'CBC', iv);
    return (0, base_xx_1.base64Encode)((0, concat_split_1.concat)((0, string_bytes_1.stringToBytes)('Salted__'), salt, encrypted));
};
exports.encryptSeed = encryptSeed;
const decryptSeed = (encryptedSeed, password, encryptionRounds) => {
    const passphrase = strengthenPassword(password, encryptionRounds);
    const encBytes = (0, base_xx_1.base64Decode)(encryptedSeed);
    const salt = encBytes.slice(8, 16);
    const key_iv = evpKdf((0, string_bytes_1.stringToBytes)(passphrase, 'raw'), salt);
    const key = (0, string_bytes_1.stringToBytes)(key_iv.slice(0, 32), 'raw');
    const iv = (0, string_bytes_1.stringToBytes)(key_iv.slice(32), 'raw');
    return (0, string_bytes_1.bytesToString)((0, encryption_1.aesDecrypt)(encBytes.slice(16), key, 'CBC', iv));
};
exports.decryptSeed = decryptSeed;
//# sourceMappingURL=seed-ecryption.js.map